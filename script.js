const translations = {
  el: {
    "brand.home": "Αρχική",
    "nav.home": "Αρχική",
    "nav.welcome": "Καλωσόρισμα",
    "nav.about": "Σχετικά",
    "nav.programs": "Προγράμματα",
    "nav.instagram": "Instagram",
    "hero.eyebrow": "Welcome",
    "hero.title": "Εξελίξτε τα Αγγλικά σας",
    "hero.lead":
      "Το English in Tourism έχει σχεδιαστεί προσεκτικά ώστε να προσφέρει στους μαθητές μια ολοκληρωμένη εμπειρία εκμάθησης, βασισμένη στην αυθεντική γλώσσα του τουρισμού και της φιλοξενίας. Το πρόγραμμα ξεπερνά τη διδασκαλία της παραδοσιακής Αγγλικής, βοηθώντας τους συμμετέχοντες να αναπτύξουν επικοινωνιακές δεξιότητες, πολιτισμική επίγνωση και ικανότητες μελέτης που χρειάζονται τόσο για ακαδημαϊκή πρόοδο όσο και για επιτυχία στον επαγγελματικό χώρο.",
    "hero.ctaPrimary": "Περισσότερα",
    "hero.ctaSecondary": "Διαβάστε περισσότερα",
    "hero.visionEyebrow": "Το Όραμα μας",
    "hero.visionTitle":
      "Το English in Tourism είναι μια εξειδικευμένη πιστοποίηση στην Αγγλική γλώσσα, σχεδιασμένη για τον σύγχρονο κόσμο του τουρισμού.",
    "welcome.overviewEyebrow": "Συνοπτικά",
    "welcome.overviewBody":
      "Μέσα από ζωντανά μαθήματα και ρεαλιστικά σενάρια, οι εκπαιδευόμενοι εξασκούνται στη χρήση της Αγγλικής όπως ακριβώς εφαρμόζεται καθημερινά σε ξενοδοχεία, εστιατόρια, ταξιδιωτικά γραφεία και τουριστικούς προορισμούς. Μαθαίνουν όχι μόνο να μιλούν και να γράφουν με αυτοπεποίθηση, αλλά και να ακούν με προσοχή, να κατανοούν αποτελεσματικά και να επικοινωνούν φυσικά με επισκέπτες από όλο τον κόσμο.",
    "welcome.visionEyebrow": "Το Όραμα μας",
    "welcome.visionBody":
      "Τα καινοτόμα προγράμματά μας απευθύνονται σε μαθητές όλων των επιπέδων, δίνοντας έμφαση στην επικοινωνία και στις πρακτικές δεξιότητες της πραγματικής ζωής. Ξεκινήστε αυτό το εμπνευσμένο ταξίδι για να αναπτύξετε τα Αγγλικά σας και να εκφράσετε τη γνήσια φιλοξενία σας.",
    "welcome.courseBody":
      "Επιπλέον, το πρόγραμμα ενισχύει τη γενική και επαγγελματική ευχέρεια στην Αγγλική, προετοιμάζοντας τους συμμετέχοντες να συντάσσουν αναφορές, να πραγματοποιούν παρουσιάσεις και να χειρίζονται πελατειακές επικοινωνίες με σαφήνεια και επαγγελματισμό.",
    "welcome.finalBody":
      "Τελικά, το English in Tourism προσφέρει μια πλήρη, δημιουργική και πρακτική μαθησιακή εμπειρία που ενδυναμώνει την αυτοπεποίθηση, ενισχύει την απασχολησιμότητα και ανοίγει τον δρόμο για νέες, συναρπαστικές ευκαιρίες στον παγκόσμιο τουριστικό κλάδο.",
    "about.eyebrow": "Γιατι να μας επιλέξετε",
    "about.title": "Απευθύνεται σε:",
    "about.intro":
      "Η πιστοποίηση English in Tourism έχει σχεδιαστεί ειδικά για άτομα που εργάζονται ή επιθυμούν να εργαστούν στον τομέα του τουρισμού και της φιλοξενίας.",
    "about.listIntro": "Απευθύνεται σε ένα ευρύ φάσμα επαγγελματιών, όπως:",
    "about.li1": "Προσωπικό εστιατορίων και ξενοδοχείων",
    "about.li2": "Μάγειρες, σερβιτόρους και προσωπικό εξυπηρέτησης",
    "about.li3": "Υπαλλήλους μπαρ και καφετεριών",
    "about.li4": "Ρεσεψιονίστ, γκρουμ και προσωπικό καθαριότητας",
    "about.li5":
      "Ανιματέρ και προσωπικό ψυχαγωγίας, καθώς και άλλους επαγγελματίες του τουριστικού κλάδου",
    "about.body1":
      "Το πρόγραμμα είναι επίσης ιδανικό για ενήλικες με βασικό επίπεδο γνώσης Αγγλικών (Α2) που επιθυμούν να αναπτύξουν τις επικοινωνιακές τους δεξιότητες και να αξιοποιήσουν ευκαιρίες στον συνεχώς αναπτυσσόμενο τομέα του τουρισμού.",
    "about.body2":
      "Με έμφαση στη χρήση της γλώσσας σε πραγματικές συνθήκες, την τουριστική ορολογία και την επικοινωνία με επισκέπτες, οι συμμετέχοντες αποκτούν αυτοπεποίθηση και ικανότητες για να προσφέρουν υπηρεσίες υψηλού επιπέδου σε διεθνές κοινό.",
    "programs.title": "Πλεονεκτήματα για τους εκπαιδευόμενους:",
    "programs.body1":
      "Μέσα από την προετοιμασία τους για τις εξετάσεις του English in Tourism, οι συμμετέχοντες δεν βελτιώνουν μόνο τις γλωσσικές τους δεξιότητες, αλλά αποκτούν και την αυτοπεποίθηση και ευχέρεια να επικοινωνούν φυσικά και αποτελεσματικά με διεθνείς επισκέπτες. Η εκπαίδευση εστιάζει σε πραγματικές συνθήκες εργασίας — από τον χαιρετισμό των πελατών και την παραγγελιοληψία, έως την παροχή οδηγιών, τη διαχείριση αιτημάτων ή και παραπόνων με ευγένεια και επαγγελματισμό.",
    "programs.body2":
      "Αποκτώντας την πιστοποίηση English in Tourism, κάθε υποψήφιος αποκτά ένα ουσιαστικό επαγγελματικό πλεονέκτημα. Η πιστοποίηση λειτουργεί ως επίσημη αναγνώριση των γλωσσικών του δεξιοτήτων και της ικανότητάς του στην εξυπηρέτηση πελατών, κάνοντάς τον πιο ανταγωνιστικό στην αγορά εργασίας, σε ξενοδοχεία, εστιατόρια, ταξιδιωτικά γραφεία, κρουαζιερόπλοια και άλλους τομείς του τουρισμού.",
    "programs.body3":
      "Τελικά, ανοίγει τον δρόμο για καλύτερες επαγγελματικές ευκαιρίες, εξέλιξη καριέρας και μια ουσιαστικότερη σύνδεση με την παγκόσμια τουριστική κοινότητα.",
    "programs.cta": "Εξευρενήστε",
    "exam.eyebrow": "Πληροφορίες για την Εξέταση",
    "exam.title": "50 ώρες καθοδηγούμενης διδασκαλίας και αυθεντική αξιολόγηση",
    "exam.introLead":
      "Το πρόγραμμα πιστοποίησης English in Tourism περιλαμβάνει περίπου 50 ώρες καθοδηγούμενης διδασκαλίας, διαδραστικής εξάσκησης και πρακτικών δραστηριοτήτων που προετοιμάζουν τους υποψηφίους για την πραγματική χρήση της Αγγλικής γλώσσας στον χώρο του τουρισμού.",
    "exam.introBody":
      "Στο τέλος του προγράμματος, οι συμμετέχοντες λαμβάνουν μέρος σε μια ολοκληρωμένη εξέταση μέσω υπολογιστή, η οποία αξιολογεί την ικανότητά τους να κατανοούν και να χρησιμοποιούν την Αγγλική γλώσσα αποτελεσματικά σε αυθεντικές επαγγελματικές συνθήκες. Η εξέταση καλύπτει και τις τέσσερις βασικές γλωσσικές δεξιότητες:",
    "exam.skill1":
      "Κατανόηση γραπτού λόγου (Reading Comprehension) – κατανόηση πληροφοριών από μενού, πινακίδες, σημειώματα και μηνύματα πελατών.",
    "exam.skill2":
      "Κατανόηση προφορικού λόγου (Listening Comprehension) – παρακολούθηση διαλόγων σε ξενοδοχεία, εστιατόρια και άλλα τουριστικά περιβάλλοντα.",
    "exam.skill3":
      "Παραγωγή προφορικού λόγου (Speaking) – συμμετοχή σε διαλόγους και παιχνίδια ρόλων που αντικατοπτρίζουν πραγματικές επαγγελματικές καταστάσεις.",
    "exam.skill4":
      "Παραγωγή γραπτού λόγου (Writing) – σύνταξη σύντομων λειτουργικών κειμένων, όπως email, επιβεβαιώσεις κρατήσεων ή απαντήσεις σε πελάτες.",
    "exam.summary1":
      "Η εξέταση είναι εξ ολοκλήρου ηλεκτρονική και βασίζεται σε σύστημα βαθμολόγησης με πόντους, ευθυγραμμισμένο με το Κοινό Ευρωπαϊκό Πλαίσιο Αναφοράς για τις Γλώσσες (CEFR), επιτρέποντας στους υποψηφίους να πιστοποιηθούν σε επίπεδα από A1 (Βασικός χρήστης) έως B2 (Άνω-Μέσος χρήστης).",
    "exam.summary2":
      "Με αυτόν τον τρόπο, το πιστοποιητικό προσφέρει μια σαφή και διεθνώς αναγνωρισμένη ένδειξη γλωσσικής επάρκειας, ενισχύοντας τις επαγγελματικές προοπτικές κάθε επιτυχόντα στον τουριστικό και ξενοδοχειακό κλάδο.",
    "contact.eyebrow": "Επικοινωνία",
    "contact.title": "Γραφεία",
    "contact.phoneLabel": "Τηλέφωνο",
    "contact.locationLabel": "Τοποθεσία",
    "contact.location": "Επαρχία Κουνάβων, 70100<br>Ηράκλειο Κρήτης",
    "footer.text": "© 2035 by English in Tourism.",
  },
  en: {
    "brand.home": "Home",
    "nav.home": "Home",
    "nav.welcome": "Welcome",
    "nav.about": "About",
    "nav.programs": "Programs",
    "nav.instagram": "Instagram",
    "hero.eyebrow": "Welcome",
    "hero.title": "Engaging Language Experience",
    "hero.lead":
      "English in Tourism has been carefully designed to immerse learners in the authentic language of the hospitality and tourism world. The program goes beyond traditional English learning. It helps participants develop the communication, cultural awareness, and study skills needed to succeed both academically and professionally.",
    "hero.ctaPrimary": "Read More",
    "hero.ctaSecondary": "Learn More",
    "hero.visionEyebrow": "Our Vision",
    "hero.visionTitle":
      "English in Tourism is a specialized English language certification designed for the dynamic world of tourism.",
    "welcome.overviewEyebrow": "Overview",
    "welcome.overviewBody":
      "Through engaging, scenario-based lessons, learners practice the kind of English used every day in hotels, restaurants, travel agencies, and tourist attractions. They learn not only how to speak and write with confidence, but also how to listen actively, read effectively, and interact naturally with guests from around the world.",
    "welcome.visionEyebrow": "Our Vision",
    "welcome.visionBody":
      "Our forward-thinking programs serve learners of all levels, emphasizing real-world communication and practical skills. Embark on this inspiring journey to elevate your English and share the true spirit of hospitality.",
    "welcome.courseBody":
      "In addition to improving general English fluency, the course strengthens academic and workplace language competence, preparing participants to handle reports, presentations, and customer interactions with clarity and professionalism.",
    "welcome.finalBody":
      "Ultimately, English in Tourism offers a complete, engaging, and practical language experience, one that builds confidence, enhances employability, and opens the door to exciting opportunities within the global tourism industry.",
    "about.eyebrow": "Why Study our Certificate",
    "about.title": "Who is it for?",
    "about.intro":
      "The English in Tourism certification is thoughtfully crafted for individuals who are either currently employed in, or aspire to enter, the tourism and hospitality industry.",
    "about.listIntro": "It serves a wide range of professionals, including:",
    "about.li1": "Restaurant and hotel personnel",
    "about.li2": "Chefs, waiters, and service staff",
    "about.li3": "Bar and cafe employees",
    "about.li4": "Receptionists, porters, and housekeeping staff",
    "about.li5": "Animation and entertainment teams, as well as other hospitality professionals",
    "about.body1":
      "The program is also ideal for adults with a basic level of English (A2) who wish to improve their communication skills and pursue opportunities in the rapidly expanding tourism sector.",
    "about.body2":
      "Through a focus on real-life language use, industry terminology, and customer interaction, participants gain the confidence and competence needed to provide exceptional service to international guests.",
    "programs.title": "Advantages for the Trainees:",
    "programs.body1":
      "Through their preparation for the English in Tourism examination, participants not only strengthen their command of the English language but also develop the confidence and fluency needed to communicate naturally and effectively with international visitors. The training focuses on real-life situations they encounter in their daily work, from greeting guests and taking orders to giving directions, handling requests, or managing complaints politely and professionally.",
    "programs.body2":
      "By obtaining the English in Tourism certificate, every candidate gains a clear professional advantage. The certification serves as an official recognition of their language skills and customer-service competence, making them more attractive to employers in hotels, restaurants, travel agencies, cruise lines, and other areas of the tourism industry.",
    "programs.body3":
      "Ultimately, it opens doors to better job opportunities, career progression, and a stronger connection with the global tourism community.",
    "programs.cta": "Discover More",
    "exam.eyebrow": "Exam Profile",
    "exam.title": "50 hours of guided instruction and authentic assessment",
    "exam.introLead":
      "The English in Tourism certification program consists of approximately 50 hours of guided instruction, interactive practice, and real-world communication tasks designed to prepare candidates for professional use of English in tourism settings.",
    "exam.introBody":
      "At the end of the course, participants take a comprehensive computer-based examination that evaluates their ability to understand and use English effectively in authentic workplace scenarios. The assessment covers all four core language skills:",
    "exam.skill1":
      "Reading Comprehension – understanding written information such as menus, signs, notices, and customer messages.",
    "exam.skill2":
      "Listening Comprehension – following spoken exchanges in hotels, restaurants, and other tourist environments.",
    "exam.skill3":
      "Speaking – engaging in role plays and dialogues that reflect real customer interactions.",
    "exam.skill4":
      "Writing – producing short, functional texts such as emails, booking confirmations, or guest replies.",
    "exam.summary1":
      "The examination uses a points-based grading system aligned with the Common European Framework of Reference (CEFR), allowing candidates to achieve certification levels ranging from A1 (Basic User) to B2 (Upper-Intermediate User).",
    "exam.summary2":
      "This transparent and internationally recognized system ensures that every successful candidate can clearly demonstrate their level of English proficiency to employers within the tourism and hospitality industry.",
    "contact.eyebrow": "Contact Us",
    "contact.title": "Offices",
    "contact.phoneLabel": "Contact",
    "contact.locationLabel": "Offices",
    "contact.location": "Kounavoi, Heraklion Crete",
    "footer.text": "© 2035 by English in Tourism.",
  },
};

const loader = document.querySelector(".page-loader");
const progressBar = document.querySelector(".scroll-progress__bar");
const nav = document.querySelector(".nav");
const navToggle = document.querySelector(".nav-toggle");
const navMenu = document.querySelector(".nav-menu");
const navLinks = [...document.querySelectorAll(".nav-menu a")];
const revealItems = [...document.querySelectorAll(".reveal")];
const parallaxTarget = document.querySelector("[data-parallax]");
const magneticButtons = [...document.querySelectorAll(".magnetic")];
const languageButtons = [...document.querySelectorAll(".language-button")];
const i18nNodes = [...document.querySelectorAll("[data-i18n]")];
const i18nHtmlNodes = [...document.querySelectorAll("[data-i18n-html]")];
const orbitRings = [...document.querySelectorAll("[data-orbit-ring]")];
const mainOrbitRing =
  document.querySelector('[data-orbit-ring="main"]') ?? orbitRings[0] ?? null;
const scrollCue = document.querySelector(".scroll-cue");

const wait = (ms) => new Promise((resolve) => window.setTimeout(resolve, ms));

if ("scrollRestoration" in window.history) {
  window.history.scrollRestoration = "manual";
}

const createOrbitDots = (ring) => {
  if (!ring) {
    return;
  }

  const isLoaderRing = ring.dataset.orbitRing === "loader";
  const introBaseDelay = isLoaderRing ? 0.5 : 0;
  ring.innerHTML = "";
  const totalDots = 44;
  const radius = 49.2;

  for (let index = 0; index < totalDots; index += 1) {
    const dot = document.createElement("span");
    const angleDeg = (360 / totalDots) * index - 90;
    const angleRad = (angleDeg * Math.PI) / 180;
    const x = 50 + Math.cos(angleRad) * radius;
    const y = 50 + Math.sin(angleRad) * radius;
    const duration = `${4.2 + (index % 7) * 0.24}s`;
    const introDelay = `${introBaseDelay + index * 0.04}s`;
    const idleDelay = `${introBaseDelay + 0.5 + index * 0.04}s`;
    const scale = `${0.82 + ((index % 4) * 0.08)}`;
    const opacity = `${0.68 + ((index % 6) * 0.05)}`;
    const pushX = (Math.cos(angleRad) * 0.035).toFixed(3);
    const pushY = (Math.sin(angleRad) * 0.035).toFixed(3);
    const idleDriftX = `${(Math.cos(angleRad) * 2.2).toFixed(2)}px`;
    const idleDriftY = `${(Math.sin(angleRad) * 2.2).toFixed(2)}px`;
    const introOffsetX = `${(-Math.cos(angleRad) * 18).toFixed(2)}px`;
    const introOffsetY = `${(-Math.sin(angleRad) * 18).toFixed(2)}px`;

    dot.className = "opening__dot";
    dot.style.setProperty("--x", `${x}%`);
    dot.style.setProperty("--y", `${y}%`);
    dot.style.setProperty("--duration", duration);
    dot.style.setProperty("--intro-delay", introDelay);
    dot.style.setProperty("--idle-delay", idleDelay);
    dot.style.setProperty("--scale", scale);
    dot.style.setProperty("--opacity", opacity);
    dot.style.setProperty("--push-x", pushX);
    dot.style.setProperty("--push-y", pushY);
    dot.style.setProperty("--idle-drift-x", idleDriftX);
    dot.style.setProperty("--idle-drift-y", idleDriftY);
    dot.style.setProperty("--intro-offset-x", introOffsetX);
    dot.style.setProperty("--intro-offset-y", introOffsetY);
    ring.appendChild(dot);
  }
};

orbitRings.forEach((ring) => createOrbitDots(ring));

if (mainOrbitRing && window.matchMedia("(prefers-reduced-motion: no-preference)").matches) {
  const dots = [...mainOrbitRing.querySelectorAll(".opening__dot")];
  const effectRange = 78;
  const maxShift = 11;
  const maxScaleBoost = 0.42;
  const easing = 0.16;
  const settleEpsilon = 0.02;
  const dotStates = dots.map(() => ({
    x: 0,
    y: 0,
    scale: 1,
    targetX: 0,
    targetY: 0,
    targetScale: 1,
  }));
  let pointerInside = false;
  let pointerX = 0;
  let pointerY = 0;
  let rafId = 0;

  const resetDotProximity = () => {
    dotStates.forEach((state) => {
      state.targetX = 0;
      state.targetY = 0;
      state.targetScale = 1;
    });
  };

  const updateTargets = () => {
    const rect = mainOrbitRing.getBoundingClientRect();

    dots.forEach((dot, index) => {
      const state = dotStates[index];
      if (!pointerInside) {
        state.targetX = 0;
        state.targetY = 0;
        state.targetScale = 1;
        return;
      }

      const dotX = (Number.parseFloat(dot.style.getPropertyValue("--x")) / 100) * rect.width;
      const dotY = (Number.parseFloat(dot.style.getPropertyValue("--y")) / 100) * rect.height;
      const dx = dotX - pointerX;
      const dy = dotY - pointerY;
      const distance = Math.hypot(dx, dy);
      const influence = Math.max(0, 1 - distance / effectRange);

      if (influence <= 0) {
        state.targetX = 0;
        state.targetY = 0;
        state.targetScale = 1;
        return;
      }

      const safeDistance = Math.max(distance, 0.001);
      state.targetX = (dx / safeDistance) * maxShift * influence;
      state.targetY = (dy / safeDistance) * maxShift * influence;
      state.targetScale = 1 + maxScaleBoost * influence;
    });
  };

  const animateProximity = () => {
    updateTargets();
    let hasMotion = false;

    dots.forEach((dot, index) => {
      const state = dotStates[index];
      state.x += (state.targetX - state.x) * easing;
      state.y += (state.targetY - state.y) * easing;
      state.scale += (state.targetScale - state.scale) * easing;

      dot.style.setProperty("--proximity-x", `${state.x.toFixed(2)}px`);
      dot.style.setProperty("--proximity-y", `${state.y.toFixed(2)}px`);
      dot.style.setProperty("--proximity-scale", state.scale.toFixed(3));

      if (
        Math.abs(state.targetX - state.x) > settleEpsilon ||
        Math.abs(state.targetY - state.y) > settleEpsilon ||
        Math.abs(state.targetScale - state.scale) > settleEpsilon
      ) {
        hasMotion = true;
      }
    });

    if (pointerInside || hasMotion) {
      rafId = window.requestAnimationFrame(animateProximity);
    } else {
      rafId = 0;
    }
  };

  const ensureAnimationLoop = () => {
    if (rafId === 0) {
      rafId = window.requestAnimationFrame(animateProximity);
    }
  };

  mainOrbitRing.addEventListener("pointermove", (event) => {
    const rect = mainOrbitRing.getBoundingClientRect();
    pointerInside = true;
    pointerX = event.clientX - rect.left;
    pointerY = event.clientY - rect.top;
    ensureAnimationLoop();
  });

  mainOrbitRing.addEventListener("pointerleave", () => {
    pointerInside = false;
    resetDotProximity();
    ensureAnimationLoop();
  });

  resetDotProximity();
  ensureAnimationLoop();
}

const setLanguage = async (lang, { animate = false } = {}) => {
  const locale = translations[lang] ? lang : "el";
  const dictionary = translations[locale];

  if (animate) {
    document.body.classList.add("is-language-switching");
    await wait(180);
  }

  i18nNodes.forEach((node) => {
    const key = node.dataset.i18n;
    if (dictionary[key]) {
      node.textContent = dictionary[key];
    }
  });

  i18nHtmlNodes.forEach((node) => {
    const key = node.dataset.i18nHtml;
    if (dictionary[key]) {
      node.innerHTML = dictionary[key];
    }
  });

  document.documentElement.lang = locale;
  document.title =
    locale === "en"
      ? "English In Tourism | English"
      : "English In Tourism | Ελληνικά";

  const description = document.querySelector('meta[name="description"]');
  if (description) {
    description.content =
      locale === "en"
        ? "English in Tourism. Certificate to improve your English in Hospitality."
        : "English in Tourism. Πιστοποίηση για βελτίωση των Αγγλικών στη φιλοξενία.";
  }

  languageButtons.forEach((button) => {
    const active = button.dataset.lang === locale;
    button.classList.toggle("is-active", active);
    button.setAttribute("aria-pressed", String(active));
  });

  localStorage.setItem("eng4sp-language", locale);

  if (animate) {
    await wait(40);
    document.body.classList.remove("is-language-switching");
  }
};

window.addEventListener("load", () => {
  if (window.location.hash) {
    window.history.replaceState(null, "", `${window.location.pathname}${window.location.search}`);
  }
  window.scrollTo(0, 0);

  const savedLanguage = localStorage.getItem("eng4sp-language") || "el";
  setLanguage(savedLanguage, { animate: false });
  window.setTimeout(() => {
    loader?.classList.add("is-hidden");
    document.body.classList.remove("is-loading");
  }, 1250);
});

languageButtons.forEach((button) => {
  button.addEventListener("click", () => {
    const nextLang = button.dataset.lang;
    const currentLang = document.documentElement.lang || "el";

    if (nextLang === currentLang) {
      return;
    }

    setLanguage(nextLang, { animate: true });
  });
});

const updateProgress = () => {
  const scrollTop = window.scrollY;
  const scrollHeight = document.documentElement.scrollHeight - window.innerHeight;
  const progress = scrollHeight > 0 ? (scrollTop / scrollHeight) * 100 : 0;
  if (progressBar) {
    progressBar.style.width = `${progress}%`;
  }
};

const updateTopState = () => {
  const nearTop = window.scrollY < 80;
  document.body.classList.toggle("is-at-top", nearTop);
  if (scrollCue) {
    scrollCue.setAttribute("aria-hidden", String(!nearTop));
  }
};

updateProgress();
updateTopState();
window.addEventListener("scroll", updateProgress, { passive: true });
window.addEventListener("scroll", updateTopState, { passive: true });

if (navToggle && navMenu) {
  const setMenuState = (open) => {
    navMenu.classList.toggle("is-open", open);
    navToggle.classList.toggle("is-open", open);
    nav?.classList.toggle("nav--open", open);
    navToggle.setAttribute("aria-expanded", String(open));
    navToggle.setAttribute("aria-label", open ? "Close menu" : "Open menu");
  };

  setMenuState(false);

  navToggle.addEventListener("click", () => {
    const open = !navMenu.classList.contains("is-open");
    setMenuState(open);
  });

  navLinks.forEach((link) => {
    link.addEventListener("click", () => {
      setMenuState(false);
    });
  });
}

const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      }
    });
  },
  {
    threshold: 0.16,
    rootMargin: "0px 0px -12% 0px",
  }
);

revealItems.forEach((item, index) => {
  item.style.transitionDelay = `${Math.min(index * 40, 280)}ms`;
  observer.observe(item);
});

const sections = navLinks
  .map((link) => document.querySelector(link.getAttribute("href")))
  .filter(Boolean);

const setActiveLink = () => {
  const offset = window.scrollY + window.innerHeight * 0.3;

  sections.forEach((section) => {
    const link = navLinks.find((item) => item.getAttribute("href") === `#${section.id}`);
    if (!link) {
      return;
    }

    const inView =
      offset >= section.offsetTop &&
      offset < section.offsetTop + section.offsetHeight;

    link.classList.toggle("is-active", inView);
  });
};

setActiveLink();
window.addEventListener("scroll", setActiveLink, { passive: true });

if (parallaxTarget && window.matchMedia("(prefers-reduced-motion: no-preference)").matches) {
  const mediaImage = parallaxTarget.querySelector("img");

  window.addEventListener("pointermove", (event) => {
    const { innerWidth, innerHeight } = window;
    const offsetX = (event.clientX / innerWidth - 0.5) * 18;
    const offsetY = (event.clientY / innerHeight - 0.5) * 18;

    parallaxTarget.style.transform = `translate3d(${offsetX * -0.2}px, ${offsetY * -0.2}px, 0)`;
    if (mediaImage) {
      mediaImage.style.transform = `scale(1.06) translate3d(${offsetX}px, ${offsetY}px, 0)`;
    }
  });
}

magneticButtons.forEach((button) => {
  button.addEventListener("pointermove", (event) => {
    const rect = button.getBoundingClientRect();
    const offsetX = event.clientX - rect.left - rect.width / 2;
    const offsetY = event.clientY - rect.top - rect.height / 2;
    button.style.transform = `translate(${offsetX * 0.08}px, ${offsetY * 0.08}px)`;
  });

  button.addEventListener("pointerleave", () => {
    button.style.transform = "";
  });
});
